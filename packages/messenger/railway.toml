[build]
buildCommand = "cd ../.. && npm install && npm run build -w packages/shared && npm run build -w packages/messenger"

[deploy]
startCommand = "node dist/bot.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
